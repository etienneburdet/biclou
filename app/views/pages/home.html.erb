<div class="container">
  <div class="row">
    <%= form_with url: search_bikes_path do |f| %>
      <%= f.input :name %>

      <%= f.submit "Submit", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>































